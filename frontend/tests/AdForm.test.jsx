import "@testing-library/jest-dom";
import { describe, test, expect } from "vitest";

describe("AdForm", () => {
  test("AdForm test file structure works", () => {
    expect(true).toBe(true);
  });
});
