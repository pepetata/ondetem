import "@testing-library/jest-dom";
import { describe, test, expect } from "vitest";

describe("AdForm Basic Tests", () => {
  test("simple math works", () => {
    expect(2 + 2).toBe(4);
  });

  test("strings work", () => {
    expect("hello").toBe("hello");
  });
});
